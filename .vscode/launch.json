{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Day 7 Part 2",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/aoc", // Replace with your actual binary name
            "args": [], // No arguments needed if you hardcode the day in your main.rs
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build", // Ensure the latest binary is built
            "sourceLanguages": ["rust"]
        }
    ]
}
